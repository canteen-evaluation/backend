<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="canteen_evaluation_backend.dao.TestMapper" >
    <!-- 这里可以不指定 db 的名字，也可以指明-->
    <select id="getAll" resultType="canteen_evaluation_backend.pojo.TestPojo">
        SELECT * FROM canteen_evaluation.table_test
    </select>
</mapper>
